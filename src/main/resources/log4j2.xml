<?xml version="1.0" encoding="UTF-8"?>
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数 -项目部署后修改自动读取，不用重启 -->
<configuration monitorInterval="5">
    <!-- 属性 -->
    <Properties>
        <!-- 日期格式化输出：%date表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度 %msg：日志消息，%n是换行符-->
        <!-- %logger{36} 表示 Logger 名字最长36个字符 -->
        <property name="LOG_PATTERN" value="%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />
        <!-- 路径 -->
        <Property name="FILE_PATH">E:/log</Property>
        <!-- 日志名称 -->
        <Property name="FILE_NAME">info.log</Property>
        <Property name="FILE_NAME_SYS">sys.log</Property>
        <Property name="FILE_NAME_API">api.log</Property>

    </Properties>

    <!-- 日志文件属性 -->
    <appenders>
        <!-- 控制台输出 -->
        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <!--控制台只输出level及其以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
        </console>

        <!--文件会打印出所有信息，这个log每次运行程序会自动清空，由append属性决定，适合临时测试用-->
        <File name="Filelog" fileName="${FILE_PATH}/test.log" append="false">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </File>

        <RollingFile name="test1RollingFile" fileName="${FILE_PATH}/${test1logRollingFile}" append="true"
                     filePattern="${FILE_PATH}/$${date:yyyy-MM}/app-%d{MM-dd-yyyy}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd 'at' HH:mm:ss z} %-5level %class{36} %L %M - %msg%xEx%n"/>
            <SizeBasedTriggeringPolicy size="4 MB"/>
        </RollingFile>

        <File name="test2File" fileName="${FILE_PATH}/${test2logFile}" append="false">
            <PatternLayout pattern="%d{HH:mm:ss} %-5level %class{36}  %L %M - %msg%xEx%n"/>
        </File>
        <RollingFile name="test2RollingFile" fileName="${FILE_PATH}/${test2logRollingFile}" append="true"
                     filePattern="log/test2/$${date:yyyy-MM}/app-%d{MM-dd-yyyy}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd 'at' HH:mm:ss z} %-5level %class{36} %L %M - %msg%xEx%n"/>
            <SizeBasedTriggeringPolicy size="4 MB"/>
        </RollingFile>
    </appenders>
    <!-- 输出项 -->
    <loggers>
        <Logger name="test1Logger" additivity="TRUE" level="ALL">
            <AppenderRef ref="test1File" level="ALL"/>
            <AppenderRef ref="test1RollingFile" level="ALL"/>
        </Logger>
        <Logger name="test2Logger" additivity="TRUE" level="ALL">
            <AppenderRef ref="test2RollingFile" level="ALL"/>
        </Logger>
    </loggers>
</configuration>